{
    "type": "UTC_offset",
    "time_zone_code": "ULN",
    "offset": 8,
    "help":
    {
        "type": "either 'UTC_offset or 'time_zone'",
        "time_zone_code": "time zone code. If type=time_zone, time_zone_code must be a valid pytz timezone",
        "offset": "time offset in hours, ignored if type=time_zone"
    }
}